# Frontend Context

Read root `AGENTS.md` first. This area will contain the React/Vite application. Feature
code must use Cortex-owned UI abstractions, generated OpenAPI client types, and the
frontend architecture documents in `docs/frontend/`; do not import PrimeReact, lucide, or
React Flow directly outside their approved adapter layers.
