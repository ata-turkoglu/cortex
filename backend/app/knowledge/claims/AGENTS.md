# Claims and Facts

Read parent `../AGENTS.md`. Own `ExtractedClaim → SupportedClaim → VerifiedFact`, validator state,
contradictions, and precedence. Model confidence alone never verifies a fact; conflicting claims
remain queryable and canonical state may be conflicted.
