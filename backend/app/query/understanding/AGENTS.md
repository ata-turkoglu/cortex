# Query Understanding

Read parent `../AGENTS.md` and the Query V2 runtime contract. Own schema-constrained semantic
interpretation, planner tier selection, candidate interpretations, and temporal meaning. Produce no
`intent` or physical engine choice; invalid provider output must be rejected or safely repaired.
Apply deterministic conversation carry-over only after schema validation, and persist any evolved
context later through the context boundary rather than from the provider call.
