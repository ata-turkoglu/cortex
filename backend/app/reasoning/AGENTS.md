# Reasoning and Composition Context

Read the root, `backend/AGENTS.md`, Query V2 runtime contract, and invariants. Choose `research/` or
`composition/`. Reuse durable workflow infrastructure and consume only reconciled trustworthy
evidence packages. Preserve claim-, sentence-, or paragraph-level provenance through every step.
